# Malcolm — Challenge 21: curl — 2026-06-08T21:01:00Z

**Status:** ✅ COMPLETE & APPROVED

**Outcome:** Built raw-socket HTTP/1.1 client (Challenge 21) with:
- Raw TCP + TLS (no net/http for protocol)
- Flags: `-X`, `-H`, `-d`, `-o`, `-I`, `-v`, `-L` (3xx redirects)
- Body framing: `Content-Length` + chunked decoder (hand-rolled, hex sizes, 0-chunk terminator)
- Test coverage: 30 pass (unit + 3 e2e over local listener)
- Live network verified: example.com, https redirect, GitHub redirect chain
- README: 326 lines, socket→bytes→HTTP teaching, CGO_ENABLED=0 toolchain note documented

**Key Learning:** Toolchain workaround for macOS — `CGO_ENABLED=0 go test ./...` required for net/crypto/tls imports (cgo LC_UUID linker mismatch with Xcode CLT). Documented in curl README; applies to all Phase 4+ networking challenges.

**Reviewer Verdict:** Ellie approved 2026-06-09 — correctness, test coverage, teaching README all verified.
