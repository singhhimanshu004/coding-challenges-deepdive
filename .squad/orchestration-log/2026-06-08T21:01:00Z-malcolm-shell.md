# Malcolm — Challenge 22: Shell (gosh) — 2026-06-08T21:01:00Z

**Status:** ✅ COMPLETE & APPROVED (Phase 3 capstone)

**Outcome:** Built working interactive Unix shell (Challenge 22) with:
- Three-stage pipeline: tokenizer (quote/escape aware) → parser (recursive-descent) → executor (fork/exec fd wiring)
- Features: pipes, redirects (>, >>, <, 2>), sequencing (;), logical (&&, ||), env expansion ($VAR/${VAR}/$?/$$), builtins (cd/pwd/exit/echo/export/type), Ctrl-C handling
- Test coverage: 33 pass (tokenizer/parser/expand/executor/builtins + real execution)
- Manual verification: cd+pwd, pipes, redirects, exit codes, builtin $? expansion — all correct
- README: 297 lines, fd-level pipeline diagram, "why cd is builtin" section, EOF hang trap explained, Python analogies

**Hard-Won Lessons (Reusable):**
1. **Pipe hang bug:** Parent must close pipe-fd copies after starting children or reader never sees EOF. Explicit "ownership rule" in `execMulti`.
2. **`cd` is a builtin:** Process state mutation — child cd changes its own dir then exits, parent unmoved.
3. **fork/exec two-step:** `Start()` ≈ fork, `Wait()` ≈ wait; gap is where you rewire fds via `cmd.Stdin/Stdout/Stderr =`.
4. **Word-part coalescing:** Unquoted chars must coalesce into one expandable word or `$MYVAR` tokenizes as separate tokens.
5. **Init-cycle gotcha:** Go dispatch-table self-reference (builtin map) requires init() population, not literal.

**Reviewer Verdict:** Ellie approved 2026-06-09 — correct implementation, teaching README, all execution tests passed. Phase 3 COMPLETE.
